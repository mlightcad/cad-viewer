---
title: 連続計測
---

# 連続計測

複数の点間の距離を連続的に計測し、各セグメントの長さを個別に表示します。

## 操作手順

1. 最初の点をクリック
2. 後続の点を順にクリック——クリックするたびに新しいセグメントが即座に生成され、長さを表示
3. **Enter** または **Esc** で終了（または始点/終点に近い位置をクリックすると自動的に閉じる）

## プレビュー

完全な折線ゴムバンドで、各セグメント中点に長さのバッジがあります。

## 制限

最大 100 点まで。隣接する点の各ペアは実際には独立した距離計測で、個別に編集できます。
